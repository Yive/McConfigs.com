<?php
$_POST['change-displayname'] = isset($_POST['change-displayname']) ? $_POST['change-displayname'] : "false";
$_POST['change-playerlist'] = isset($_POST['change-playerlist']) ? $_POST['change-playerlist'] : "false";
$_POST['ignore-colors-in-max-nick-length'] = isset($_POST['ignore-colors-in-max-nick-length']) ? $_POST['ignore-colors-in-max-nick-length'] : "false";
$_POST['force-disable-teleport-safety'] = isset($_POST['force-disable-teleport-safety']) ? $_POST['force-disable-teleport-safety'] : "false";
$_POST['teleport-safety'] = isset($_POST['teleport-safety']) ? $_POST['teleport-safety'] : "false";
$_POST['warn-on-smite'] = isset($_POST['warn-on-smite']) ? $_POST['warn-on-smite'] : "false";
$_POST['drop-items-if-full'] = isset($_POST['drop-items-if-full']) ? $_POST['drop-items-if-full'] : "false";
$_POST['cancel-afk-on-interact'] = isset($_POST['cancel-afk-on-interact']) ? $_POST['cancel-afk-on-interact'] : "false";
$_POST['cancel-afk-on-move'] = isset($_POST['cancel-afk-on-move']) ? $_POST['cancel-afk-on-move'] : "false";
$_POST['death-messages'] = isset($_POST['death-messages']) ? $_POST['death-messages'] : "false";
$_POST['repair-enchanted'] = isset($_POST['repair-enchanted']) ? $_POST['repair-enchanted'] : "false";
$_POST['update-bed-at-daytime'] = isset($_POST['update-bed-at-daytime']) ? $_POST['update-bed-at-daytime'] : "false";
$_POST['non-ess-in-help'] = isset($_POST['non-ess-in-help']) ? $_POST['non-ess-in-help'] : "false";
$_POST['hide-permissionless-help'] = isset($_POST['hide-permissionless-help']) ? $_POST['hide-permissionless-help'] : "false";
$_POST['fire-spread'] = isset($_POST['fire-spread']) ? $_POST['fire-spread'] : "false";
$_POST['lava-fire-spread'] = isset($_POST['lava-fire-spread']) ? $_POST['lava-fire-spread'] : "false";
$_POST['lightning-fire-spread'] = isset($_POST['lightning-fire-spread']) ? $_POST['lightning-fire-spread'] : "false";
$_POST['enderdragon-blockdamage'] = isset($_POST['enderdragon-blockdamage']) ? $_POST['enderdragon-blockdamage'] : "false";
$_POST['build-privilege'] = isset($_POST['build-privilege']) ? $_POST['build-privilege'] : "false";
$_POST['use-privilege'] = isset($_POST['use-privilege']) ? $_POST['use-privilege'] : "false";
$_POST['warn-on-build-disallow'] = isset($_POST['warn-on-build-disallow']) ? $_POST['warn-on-build-disallow'] : "false";
$_POST['add-prefix-suffix'] = isset($_POST['add-prefix-suffix']) ? $_POST['add-prefix-suffix'] : "false";
$_POST['permission-based-item-spawn'] = isset($_POST['permission-based-item-spawn']) ? $_POST['permission-based-item-spawn'] : "false";
$_POST['kits-items'] = isset($_POST['kits-items']) ? $_POST['kits-items'] : "false";
$_POST['per-warp-permission'] = isset($_POST['per-warp-permission']) ? $_POST['per-warp-permission'] : "false";
$_POST['list-groups'] = isset($_POST['list-groups']) ? $_POST['list-groups'] : "false";
$_POST['debug'] = isset($_POST['debug']) ? $_POST['debug'] : "false";
$_POST['locale-enabled'] = isset($_POST['locale-enabled']) ? $_POST['locale-enabled'] : "false";
$_POST['locale'] = isset($_POST['locale']) ? $_POST['locale'] : "false";
$_POST['remove-god-on-disconnect'] = isset($_POST['remove-god-on-disconnect']) ? $_POST['remove-god-on-disconnect'] : "false";
$_POST['freeze-afk-players'] = isset($_POST['freeze-afk-players']) ? $_POST['freeze-afk-players'] : "false";
$_POST['disable-item-pickup-while-afk'] = isset($_POST['disable-item-pickup-while-afk']) ? $_POST['disable-item-pickup-while-afk'] : "false";
$_POST['allow-silent-join-quit'] = isset($_POST['allow-silent-join-quit']) ? $_POST['allow-silent-join-quit'] : "false";
$_POST['world-teleport-permissions'] = isset($_POST['world-teleport-permissions']) ? $_POST['world-teleport-permissions'] : "false";
$_POST['unsafe-enchantments'] = isset($_POST['unsafe-enchantments']) ? $_POST['unsafe-enchantments'] : "false";
$_POST['register-back-in-listener'] = isset($_POST['register-back-in-listener']) ? $_POST['register-back-in-listener'] : "false";
$_POST['min-fly-speed'] = isset($_POST['min-fly-speed']) ? $_POST['min-fly-speed'] : "false";
$_POST['world-home-permissions'] = isset($_POST['world-home-permissions']) ? $_POST['world-home-permissions'] : "false";
$_POST['economy-log-enabled'] = isset($_POST['economy-log-enabled']) ? $_POST['economy-log-enabled'] : "false";
$_POST['chat-group-enabled'] = isset($_POST['chat-group-enabled']) ? $_POST['chat-group-enabled'] : "false";
$_POST['lava-flow'] = isset($_POST['lava-flow']) ? $_POST['lava-flow'] : "false";
$_POST['water-flow'] = isset($_POST['water-flow']) ? $_POST['water-flow'] : "false";
$_POST['water-bucket-flow'] = isset($_POST['water-bucket-flow']) ? $_POST['water-bucket-flow'] : "false";
$_POST['flint-fire'] = isset($_POST['flint-fire']) ? $_POST['flint-fire'] : "false";
$_POST['portal-creation'] = isset($_POST['portal-creation']) ? $_POST['portal-creation'] : "false";
$_POST['tnt-explosion'] = isset($_POST['tnt-explosion']) ? $_POST['tnt-explosion'] : "false";
$_POST['tnt-playerdamage'] = isset($_POST['tnt-playerdamage']) ? $_POST['tnt-playerdamage'] : "false";
$_POST['tnt-minecart-explosion'] = isset($_POST['tnt-minecart-explosion']) ? $_POST['tnt-minecart-explosion'] : "false";
$_POST['tnt-minecart-playerdamage'] = isset($_POST['tnt-minecart-playerdamage']) ? $_POST['tnt-minecart-playerdamage'] : "false";
$_POST['fireball-explosion'] = isset($_POST['fireball-explosion']) ? $_POST['fireball-explosion'] : "false";
$_POST['fireball-fire'] = isset($_POST['fireball-fire']) ? $_POST['fireball-fire'] : "false";
$_POST['fireball-playerdamage'] = isset($_POST['fireball-playerdamage']) ? $_POST['fireball-playerdamage'] : "false";
$_POST['witherskull-explosion'] = isset($_POST['witherskull-explosion']) ? $_POST['witherskull-explosion'] : "false";
$_POST['witherskull-playerdamage'] = isset($_POST['witherskull-playerdamage']) ? $_POST['witherskull-playerdamage'] : "false";
$_POST['wither-spawnexplosion'] = isset($_POST['wither-spawnexplosion']) ? $_POST['wither-spawnexplosion'] : "false";
$_POST['wither-blockreplace'] = isset($_POST['wither-blockreplace']) ? $_POST['wither-blockreplace'] : "false";
$_POST['creeper-explosion'] = isset($_POST['creeper-explosion']) ? $_POST['creeper-explosion'] : "false";
$_POST['creeper-playerdamage'] = isset($_POST['creeper-playerdamage']) ? $_POST['creeper-playerdamage'] : "false";
$_POST['creeper-blockdamage'] = isset($_POST['creeper-blockdamage']) ? $_POST['creeper-blockdamage'] : "false";
$_POST['enderman-pickup'] = isset($_POST['enderman-pickup']) ? $_POST['enderman-pickup'] : "false";
$_POST['villager-death'] = isset($_POST['villager-death']) ? $_POST['villager-death'] : "false";
$_POST['entitytarget'] = isset($_POST['entitytarget']) ? $_POST['entitytarget'] : "false";
$_POST['spawn-creeper'] = isset($_POST['spawn-creeper']) ? $_POST['spawn-creeper'] : "false";
$_POST['spawn-skeleton'] = isset($_POST['spawn-skeleton']) ? $_POST['spawn-skeleton'] : "false";
$_POST['spawn-spider'] = isset($_POST['spawn-spider']) ? $_POST['spawn-spider'] : "false";
$_POST['spawn-giant'] = isset($_POST['spawn-giant']) ? $_POST['spawn-giant'] : "false";
$_POST['spawn-zombie'] = isset($_POST['spawn-zombie']) ? $_POST['spawn-zombie'] : "false";
$_POST['spawn-slime'] = isset($_POST['spawn-slime']) ? $_POST['spawn-slime'] : "false";
$_POST['spawn-ghast'] = isset($_POST['spawn-ghast']) ? $_POST['spawn-ghast'] : "false";
$_POST['spawn-pig_zombie'] = isset($_POST['spawn-pig_zombie']) ? $_POST['spawn-pig_zombie'] : "false";
$_POST['spawn-enderman'] = isset($_POST['spawn-enderman']) ? $_POST['spawn-enderman'] : "false";
$_POST['spawn-cave_spider'] = isset($_POST['spawn-cave_spider']) ? $_POST['spawn-cave_spider'] : "false";
$_POST['spawn-silverfish'] = isset($_POST['spawn-silverfish']) ? $_POST['spawn-silverfish'] : "false";
$_POST['spawn-blaze'] = isset($_POST['spawn-blaze']) ? $_POST['spawn-blaze'] : "false";
$_POST['spawn-magma_cube'] = isset($_POST['spawn-magma_cube']) ? $_POST['spawn-magma_cube'] : "false";
$_POST['spawn-ender_dragon'] = isset($_POST['spawn-ender_dragon']) ? $_POST['spawn-ender_dragon'] : "false";
$_POST['spawn-pig'] = isset($_POST['spawn-pig']) ? $_POST['spawn-pig'] : "false";
$_POST['spawn-sheep'] = isset($_POST['spawn-sheep']) ? $_POST['spawn-sheep'] : "false";
$_POST['spawn-cow'] = isset($_POST['spawn-cow']) ? $_POST['spawn-cow'] : "false";
$_POST['spawn-chicken'] = isset($_POST['spawn-chicken']) ? $_POST['spawn-chicken'] : "false";
$_POST['spawn-squid'] = isset($_POST['spawn-squid']) ? $_POST['spawn-squid'] : "false";
$_POST['spawn-wolf'] = isset($_POST['spawn-wolf']) ? $_POST['spawn-wolf'] : "false";
$_POST['spawn-mushroom_cow'] = isset($_POST['spawn-mushroom_cow']) ? $_POST['spawn-mushroom_cow'] : "false";
$_POST['spawn-snowman'] = isset($_POST['spawn-snowman']) ? $_POST['spawn-snowman'] : "false";
$_POST['spawn-ocelot'] = isset($_POST['spawn-ocelot']) ? $_POST['spawn-ocelot'] : "false";
$_POST['spawn-iron_golem'] = isset($_POST['spawn-iron_golem']) ? $_POST['spawn-iron_golem'] : "false";
$_POST['spawn-villager'] = isset($_POST['spawn-villager']) ? $_POST['spawn-villager'] : "false";
$_POST['spawn-wither'] = isset($_POST['spawn-wither']) ? $_POST['spawn-wither'] : "false";
$_POST['spawn-bat'] = isset($_POST['spawn-bat']) ? $_POST['spawn-bat'] : "false";
$_POST['spawn-witch'] = isset($_POST['spawn-witch']) ? $_POST['spawn-witch'] : "false";
$_POST['spawn-horse'] = isset($_POST['spawn-horse']) ? $_POST['spawn-horse'] : "false";
$_POST['disable-fall-damage'] = isset($_POST['disable-fall-damage']) ? $_POST['disable-fall-damage'] : "false";
$_POST['disable-pvp-damage'] = isset($_POST['disable-pvp-damage']) ? $_POST['disable-pvp-damage'] : "false";
$_POST['disable-drowning'] = isset($_POST['disable-drowning']) ? $_POST['disable-drowning'] : "false";
$_POST['disable-suffocation'] = isset($_POST['disable-suffocation']) ? $_POST['disable-suffocation'] : "false";
$_POST['disable-lava-damage'] = isset($_POST['disable-lava-damage']) ? $_POST['disable-lava-damage'] : "false";
$_POST['disable-projectile-damage'] = isset($_POST['disable-projectile-damage']) ? $_POST['disable-projectile-damage'] : "false";
$_POST['disable-contact-damage'] = isset($_POST['disable-contact-damage']) ? $_POST['disable-contact-damage'] : "false";
$_POST['disable-fire-damage'] = isset($_POST['disable-fire-damage']) ? $_POST['disable-fire-damage'] : "false";
$_POST['disable-lightning-damage'] = isset($_POST['disable-lightning-damage']) ? $_POST['disable-lightning-damage'] : "false";
$_POST['disable-wither-damage'] = isset($_POST['disable-wither-damage']) ? $_POST['disable-wither-damage'] : "false";
$_POST['disable-storms'] = isset($_POST['disable-storms']) ? $_POST['disable-storms'] : "false";
$_POST['disable-thunder'] = isset($_POST['disable-thunder']) ? $_POST['disable-thunder'] : "false";
$_POST['disable-lightning'] = isset($_POST['disable-lightning']) ? $_POST['disable-lightning'] : "false";
$_POST['respawn-at-home'] = isset($_POST['respawn-at-home']) ? $_POST['respawn-at-home'] : "false";
$_POST['enabledSigns-color'] = isset($_POST['enabledSigns-color']) ? $_POST['enabledSigns-color'] : "false";
$_POST['enabledSigns-balance'] = isset($_POST['enabledSigns-balance']) ? $_POST['enabledSigns-balance'] : "false";
$_POST['enabledSigns-buy'] = isset($_POST['enabledSigns-buy']) ? $_POST['enabledSigns-buy'] : "false";
$_POST['enabledSigns-sell'] = isset($_POST['enabledSigns-sell']) ? $_POST['enabledSigns-sell'] : "false";
$_POST['enabledSigns-trade'] = isset($_POST['enabledSigns-trade']) ? $_POST['enabledSigns-trade'] : "false";
$_POST['enabledSigns-free'] = isset($_POST['enabledSigns-free']) ? $_POST['enabledSigns-free'] : "false";
$_POST['enabledSigns-disposal'] = isset($_POST['enabledSigns-disposal']) ? $_POST['enabledSigns-disposal'] : "false";
$_POST['enabledSigns-warp'] = isset($_POST['enabledSigns-warp']) ? $_POST['enabledSigns-warp'] : "false";
$_POST['enabledSigns-kit'] = isset($_POST['enabledSigns-kit']) ? $_POST['enabledSigns-kit'] : "false";
$_POST['enabledSigns-mail'] = isset($_POST['enabledSigns-mail']) ? $_POST['enabledSigns-mail'] : "false";
$_POST['enabledSigns-enchant'] = isset($_POST['enabledSigns-enchant']) ? $_POST['enabledSigns-enchant'] : "false";
$_POST['enabledSigns-gamemode'] = isset($_POST['enabledSigns-gamemode']) ? $_POST['enabledSigns-gamemode'] : "false";
$_POST['enabledSigns-heal'] = isset($_POST['enabledSigns-heal']) ? $_POST['enabledSigns-heal'] : "false";
$_POST['enabledSigns-info'] = isset($_POST['enabledSigns-info']) ? $_POST['enabledSigns-info'] : "false";
$_POST['enabledSigns-spawnmob'] = isset($_POST['enabledSigns-spawnmob']) ? $_POST['enabledSigns-spawnmob'] : "false";
$_POST['enabledSigns-repair'] = isset($_POST['enabledSigns-repair']) ? $_POST['enabledSigns-repair'] : "false";
$_POST['enabledSigns-time'] = isset($_POST['enabledSigns-time']) ? $_POST['enabledSigns-time'] : "false";
$_POST['enabledSigns-weather'] = isset($_POST['enabledSigns-weather']) ? $_POST['enabledSigns-weather'] : "false";
$_POST['milk-bucket-easter-egg'] = isset($_POST['milk-bucket-easter-egg']) ? $_POST['milk-bucket-easter-egg'] : "false";
$_POST['last-message-reply-recipient'] = isset($_POST['last-message-reply-recipient']) ? $_POST['last-message-reply-recipient'] : "false";
$_POST['teleport-to-center'] = isset($_POST['teleport-to-center']) ? $_POST['teleport-to-center'] : "false";
$_POST['spawn-on-join'] = isset($_POST['spawn-on-join']) ? $_POST['spawn-on-join'] : "false";

$output = "############################################################
# +------------------------------------------------------+ #
# |              Generated at McConfigs.com              | #
# +------------------------------------------------------+ #
############################################################


############################################################
# +------------------------------------------------------+ #
# |                       Notes                          | #
# +------------------------------------------------------+ #
############################################################
 
# If you want to use special characters in this document, such as accented letters, you MUST save the file as UTF-8, not ANSI.
# If you receive an error when Essentials loads, ensure that:
#   - No tabs are present: YAML only allows spaces
#   - Indents are correct: YAML hierarchy is based entirely on indentation
#   - You have \"escaped\" all apostrophes in your text: If you want to write \"don't\", for example, write \"don''t\" instead (note the doubled apostrophe)
#   - Text with symbols is enclosed in single or double quotation marks
 
# If you have problems join the Essentials help support channel: http://tiny.cc/EssentialsChat
 
############################################################
# +------------------------------------------------------+ #
# |                 Essentials (Global)                  | #
# +------------------------------------------------------+ #
############################################################
 
# A color code between 0-9 or a-f. Set to 'none' to disable.
ops-name-color: '".$_POST['ops-name-color']."'
 
# The character(s) to prefix all nicknames, so that you know they are not true usernames.
nickname-prefix: '".$_POST['nickname-prefix']."'
 
# The maximum length allowed in nicknames. The nickname prefix is included in this.
max-nick-length: ".$_POST['max-nick-length']."

# When this option is enabled, nickname length checking will exclude color codes in player names.
# ie: \"&6Notch\" has 7 characters (2 are part of a color code), a length of 5 is used when this option is set to true
ignore-colors-in-max-nick-length: ".$_POST['ignore-colors-in-max-nick-length']."
 
# Disable this if you have any other plugin, that modifies the displayname of a user.
change-displayname: ".$_POST['change-displayname']."
 
# When this option is enabled, the (tab) player list will be updated with the displayname.
# The value of change-displayname (above) has to be true.
change-playerlist: ".$_POST['change-playerlist']."
 
# When EssentialsChat.jar isn't used, force essentials to add the prefix and suffix from permission plugins to displayname.
# This setting is ignored if EssentialsChat.jar is used, and defaults to 'true'.
# The value of change-displayname (above) has to be true.
# Do not edit this setting unless you know what you are doing!
add-prefix-suffix: ".$_POST['add-prefix-suffix']."
 
# If the teleport destination is unsafe, should players be teleported to the nearest safe location?
# If this is set to true, Essentials will attempt to teleport players close to the intended destination.
# If this is set to false, attempted teleports to unsafe locations will be cancelled with a warning.
teleport-safety: ".$_POST['teleport-safety']."

# This forcefully disables teleport safety checks without a warning if attempting to teleport to unsafe locations.
# teleport-safety and this option need to be set to true to force teleportation to dangerous locations.
force-disable-teleport-safety: ".$_POST['force-disable-teleport-safety']."

# The delay, in seconds, required between /home, /tp, etc.
teleport-cooldown: ".$_POST['teleport-cooldown']."
 
# The delay, in seconds, before a user actually teleports.  If the user moves or gets attacked in this timeframe, the teleport is cancelled.
teleport-delay: ".$_POST['teleport-delay']."
 
# The delay, in seconds, a player can't be attacked by other players after they have been teleported by a command.
# This will also prevent the player attacking other players.
teleport-invulnerability: ".$_POST['teleport-invulnerability']."

# Whether to make all teleportations go to the center of the block; where the x and z coordinates decimal become .5
teleport-to-center: ".$_POST['teleport-to-center']."

# The delay, in seconds, required between /heal or /feed attempts.
heal-cooldown: ".$_POST['heal-cooldown']."

# Near Radius
# The default radius with /near
# Used to use chat radius but we are going to make it separate.
near-radius: ".$_POST['near-radius']."

# What to prevent from /item and /give.
# e.g item-spawn-blacklist: 10,11,46
item-spawn-blacklist: ".$_POST['item-spawn-blacklist']."

# Set this to true if you want permission based item spawn rules.
# Note: The blacklist above will be ignored then.
# Example permissions (these go in your permissions manager):
#  - essentials.itemspawn.item-all
#  - essentials.itemspawn.item-[itemname]
#  - essentials.itemspawn.item-[itemid]
#  - essentials.give.item-all
#  - essentials.give.item-[itemname]
#  - essentials.give.item-[itemid]
#  - essentials.unlimited.item-all
#  - essentials.unlimited.item-[itemname]
#  - essentials.unlimited.item-[itemid]
#  - essentials.unlimited.item-bucket # Unlimited liquid placing
#
# For more information, visit http://wiki.ess3.net/wiki/Command_Reference/ICheat#Item.2FGive
permission-based-item-spawn: ".$_POST['permission-based-item-spawn']."
 
# Mob limit on the /spawnmob command per execution.
spawnmob-limit: ".$_POST['spawnmob-limit']."
 
# Shall we notify users when using /lightning?
warn-on-smite: ".$_POST['warn-on-smite']."

# Shall we drop items instead of adding to inventory if the target inventory is full?
drop-items-if-full: ".$_POST['drop-items-if-full']."

# Essentials Mail Notification
# Should we notify players if they have no new mail?
notify-no-new-mail: ".$_POST['notify-no-new-mail']."
 
# The motd and rules are now configured in the files motd.txt and rules.txt.
 
# When a command conflicts with another plugin, by default, Essentials will try to force the OTHER plugin to take priority.
# Commands in this list, will tell Essentials to 'not give up' the command to other plugins.
# In this state, which plugin 'wins' appears to be almost random.
#
# If you have two plugin with the same command and you wish to force Essentials to take over, you need an alias.
# To force essentials to take 'god' alias 'god' to 'egod'.
# See http://wiki.bukkit.org/Commands.yml#aliases for more information.
 
overridden-commands:
";
foreach($_POST['overridden-commands'] as $a => $b){
  if(!empty($_POST['overridden-commands'][$a])) {
    $output .= "  - ".$_POST['overridden-commands'][$a]."\n";
  } else {
    $output .= "#  - god";
  }
}
 
$output .= "

# Disabling commands here will prevent Essentials handling the command, this will not affect command conflicts.
# You should not have to disable commands used in other plugins, they will automatically get priority.
# See http://wiki.bukkit.org/Commands.yml#aliases to map commands to other plugins.
disabled-commands:
#  - nick";
foreach($_POST['disabled-commands'] as $a => $b){
  if(!empty($_POST['disabled-commands'][$a])) {
    $output .= "  - ".$_POST['disabled-commands'][$a]."\n";
  }
}
 
$output .= "

# These commands will be shown to players with socialSpy enabled.
# You can add commands from other plugins you may want to track or
# remove commands that are used for something you dont want to spy on.
# Set - '*' in order to listen on all possible commands.
socialspy-commands:
";
foreach($_POST['socialspy-commands'] as $a => $b){
  if(!empty($_POST['socialspy-commands'][$a])) {
    $output .= "  - ".$_POST['socialspy-commands'][$a]."\n";
  }
}
 
$output .= "

# Mute Commands
# These commands will be disabled when a player is muted.
# Use '*' to disable every command.
# Essentials already disabled Essentials messaging commands by default.
# It only cares about the root command, not args after that (it sees /f chat the same as /f)
mute-commands:
";
foreach($_POST['mute-commands'] as $a => $b){
  if(!empty($_POST['mute-commands'][$a])) {
    $output .= "  - ".$_POST['mute-commands'][$a]."\n";
  }
}
 
$output .= "
 # - '*'

# If you do not wish to use a permission system, you can define a list of 'player perms' below.
# This list has no effect if you are using a supported permissions system.
# If you are using an unsupported permissions system, simply delete this section.
# Whitelist the commands and permissions you wish to give players by default (everything else is op only).
# These are the permissions without the \"essentials.\" part.
player-commands:
";
foreach($_POST['player-commands'] as $a => $b){
  if(!empty($_POST['player-commands'][$a])) {
    $output .= "  - ".$_POST['player-commands'][$a]."\n";
  }
}
 
$output .= "

# When this option is enabled, one-time use kits (ie. delay < 0) will be
# removed from the /kit list when a player can no longer use it
skip-used-one-time-kits-from-kit-list: ".$_POST['skip-used-one-time-kits-from-kit-list']."

# Note: All items MUST be followed by a quantity!
# All kit names should be lower case, and will be treated as lower in permissions/costs.
# Syntax: - itemID[:DataValue/Durability] Amount [Enchantment:Level].. [itemmeta:value]...
# For Item Meta information visit http://wiki.ess3.net/wiki/Item_Meta
# 'delay' refers to the cooldown between how often you can use each kit, measured in seconds.
# Set delay to -1 for a one time kit.
# For more information, visit http://wiki.ess3.net/wiki/Kits
kits:
";
foreach($_POST['kits-name'] as $a => $b){
  if(!empty($_POST['kits-name'][$a])) {
    $output .= "  ".$_POST['kits-name'][$a].":\n";
  }
  if(!empty($_POST['kits-delay'][$a])) {
    $output .= "    delay: ".$_POST['kits-delay'][$a]."\n";
  }
if(!empty($_POST['kits-name'][$a])) {
  $output .= "    items:\n";
}
  $a = $a+1;
  foreach ($_POST['kits-items'][$a] as $c => $d) {
    if(!empty($_POST['kits-items'][$a][$c])) {
      $output .= "      - ".$_POST['kits-items'][$a][$c]."\n";
    }
  }
}
 
$output .= "
# Essentials Sign Control
# See http://wiki.ess3.net/wiki/Sign_Tutorial for instructions on how to use these.
# To enable signs, remove # symbol. To disable all signs, comment/remove each sign.
# Essentials colored sign support will be enabled when any sign types are enabled.
# Color is not an actual sign, it's for enabling using color codes on signs, when the correct permissions are given.
 
enabledSigns:
";

$enabledSigns_array = array('color', 'balance', 'buy', 'sell', 'trade', 'free', 'disposal', 'warp', 'kit', 'mail', 'enchant', 'gamemode', 'heal', 'info', 'spawnmob', 'repair', 'time', 'weather');

foreach ($enabledSigns_array as $key => $sign) {
  if($_POST['enabledSigns-'.$sign] == "true") {
    $output .= "  - ".$sign."\n";
  }
  if($_POST['enabledSigns-'.$sign] == "false") {
    $output .= "  #- ".$sign."\n";
  }
}

$output .= "
# How many times per second can Essentials signs be interacted with per player.
# Values should be between 1-20, 20 being virtually no lag protection.
# Lower numbers will reduce the possibility of lag, but may annoy players.
sign-use-per-second: ".$_POST['sign-use-per-second']."
 
# Backup runs a batch/bash command while saving is disabled.
backup:
  # Interval in minutes.
  interval: ".$_POST['backup-interval']."
  # Unless you add a valid backup command or script here, this feature will be useless.
  # Use 'save-all' to simply force regular world saving without backup.
  #command: 'rdiff-backup World1 backups/World1'
 
# Set this true to enable permission per warp.
per-warp-permission: ".$_POST['per-warp-permission']."
 
# Sort output of /list command by groups.
# You can hide and merge the groups displayed in /list by defining the desired behaviour here.
# Detailed instructions and examples can be found on the wiki: http://wiki.ess3.net/wiki/List
list:";
if(!empty($_POST['list-groups'])) {
foreach($_POST['list-name'] as $a => $b){
  if(!empty($_POST['list-name'][$a])) {
    $output .= "\n    ".$_POST['list-name'][$a].":";
  }
foreach ($_POST['list-groups'] as $a => $b){
  foreach ($_POST['list-groups'][$a] as $c => $d) {
    if(!empty($_POST['list-groups'][$a][$c])) {
      $output .= " ".$_POST['list-groups'][$a][$c];
    }
  }
}
}
} else {
  $output .= "
    Players: '*'\n";
}
$output .= " 
# More output to the console.
debug: ".$_POST['debug']."
 
# Set the locale for all messages.
# If you don't set this, the default locale of the server will be used.
# For example, to set language to English, set locale to en, to use the file \"messages_en.properties\".
# Don't forget to remove the # in front of the line.
# For more information, visit http://wiki.ess3.net/wiki/Locale
";
if($_POST['locale-enabled'] == "true") {
 $output .= "locale: ".$_POST['locale']."\n";
}
if($_POST['locale-enabled'] == "false") {
 $output .= "#locale: en\n";
}

$output .= " 
# Turn off god mode when people leave the server.
remove-god-on-disconnect: ".$_POST['remove-god-on-disconnect']."
 
# Auto-AFK
# After this timeout in seconds, the user will be set as AFK.
# This feature requires the player to have essentials.afk.auto node.
# Set to -1 for no timeout.
auto-afk: ".$_POST['auto-afk']."
 
# Auto-AFK Kick
# After this timeout in seconds, the user will be kicked from the server.
# essentials.afk.kickexempt node overrides this feature.
# Set to -1 for no timeout.
auto-afk-kick: ".$_POST['auto-afk-kick']."
 
# Set this to true, if you want to freeze the player, if the player is AFK.
# Other players or monsters can't push the player out of AFK mode then.
# This will also enable temporary god mode for the AFK player.
# The player has to use the command /afk to leave the AFK mode.
freeze-afk-players: ".$_POST['freeze-afk-players']."
 
# When the player is AFK, should he be able to pickup items?
# Enable this, when you don't want people idling in mob traps.
disable-item-pickup-while-afk:  ".$_POST['disable-item-pickup-while-afk']."
 
# This setting controls if a player is marked as active on interaction.
# When this setting is false, the player would need to manually un-AFK using the /afk command.
cancel-afk-on-interact: ".$_POST['cancel-afk-on-interact']."
 
# Should we automatically remove afk status when a player moves?
# Player will be removed from AFK on chat/command regardless of this setting.
# Disable this to reduce server lag.
cancel-afk-on-move: ".$_POST['cancel-afk-on-move']."
 
# You can disable the death messages of Minecraft here.
death-messages: ".$_POST['death-messages']."
 
# Should players with permissions be able to join and part silently?
# You can control this with essentials.silentjoin and essentials.silentquit permissions if it is enabled.
# In addition, people with essentials.silentjoin.vanish will be vanished on join.
allow-silent-join-quit: ".$_POST['allow-silent-join-quit']."
 
# You can set a custom join message here, set to \"none\" to disable.
# You may use color codes, use {USERNAME} the player's name or {PLAYER} for the player's displayname.
custom-join-message: \"".$_POST['custom-join-message']."\"
 
# You can set a custom quit message here, set to \"none\" to disable.
# You may use color codes, use {USERNAME} the player's name or {PLAYER} for the player's displayname.
custom-quit-message: \"".$_POST['custom-quit-message']."\"
 
# Add worlds to this list, if you want to automatically disable god mode there.
no-god-in-worlds:
";
foreach($_POST['no-godmode-worlds'] as $a => $b){
  if(!empty($_POST['no-godmode-worlds'][$a])) {
    $output .= "  - ".$_POST['no-godmode-worlds'][$a]."\n";
  }
  if(empty($_POST['no-godmode-worlds'][$a])) {
    $output .= "#  - world_nether\n";
  }
}

$output .= " 
# Set to true to enable per-world permissions for teleporting between worlds with essentials commands.
# This applies to /world, /back, /tp[a|o][here|all], but not warps.
# Give someone permission to teleport to a world with essentials.worlds.<worldname>
# This does not affect the /home command, there is a separate toggle below for this.
world-teleport-permissions: ".$_POST['world-teleport-permissions']."
 
# The number of items given if the quantity parameter is left out in /item or /give.
# If this number is below 1, the maximum stack size size is given. If over-sized stacks.
# are not enabled, any number higher than the maximum stack size results in more than one stack.
default-stack-size: ".$_POST['default-stack-size']."
 
# Over-sized stacks are stacks that ignore the normal max stack size.
# They can be obtained using /give and /item, if the player has essentials.oversizedstacks permission.
# How many items should be in an over-sized stack?
oversized-stacksize: ".$_POST['oversized-stacksize']."
 
# Allow repair of enchanted weapons and armor.
# If you set this to false, you can still allow it for certain players using the permission.
# essentials.repair.enchanted
repair-enchanted: ".$_POST['repair-enchanted']."
 
# Allow 'unsafe' enchantments in kits and item spawning.
# Warning: Mixing and overleveling some enchantments can cause issues with clients, servers and plugins.
unsafe-enchantments: ".$_POST['unsafe-enchantments']."
 
#Do you want Essentials to keep track of previous location for /back in the teleport listener?
#If you set this to true any plugin that uses teleport will have the previous location registered.
register-back-in-listener: ".$_POST['register-back-in-listener']."
 
#Delay to wait before people can cause attack damage after logging in.
login-attack-delay: ".$_POST['login-attack-delay']."
 
#Set the max fly speed, values range from 0.1 to 1.0
max-fly-speed: ".$_POST['max-fly-speed']."
 
#Set the max walk speed, values range from 0.1 to 1.0
max-walk-speed: ".$_POST['min-fly-speed']."
 
#Set the maximum amount of mail that can be sent within a minute.
mails-per-minute: ".$_POST['mails-per-minute']."
 
# Set the maximum time /tempban can be used for in seconds.
# Set to -1 to disable, and essentials.tempban.unlimited can be used to override.
max-tempban-time: ".$_POST['max-tempban-time']."

# Changes /reply functionality. If true, /r goes to the person you messaged last, otherwise the first person that messaged you. 
# If false, /r goes to the last person that messaged you.
last-message-reply-recipient: ".$_POST['last-message-reply-recipient']."

# If last-message-reply-recipient is true, this specifies the duration, in seconds, that would need to elapse for the 
# reply-recipient to update when receiving a message.
# Default is 180 (3 minutes)
last-message-reply-recipient-timeout: ".$_POST['last-message-reply-recipient-timeout']."

# Toggles whether or not right clicking mobs with a milk bucket turns them into a baby.
milk-bucket-easter-egg: ".$_POST['milk-bucket-easter-egg']."

# Toggles whether or not the fly status message should be sent to players on join
send-fly-enable-on-join: ".$_POST['send-fly-enable-on-join']."

# Set to true to enable per-world permissions for setting time for individual worlds with essentials commands.
# This applies to /time, /day, /eday, /night, /enight, /etime.
# Give someone permission to teleport to a world with essentials.time.world.<worldname>.
world-time-permissions: false
 
############################################################
# +------------------------------------------------------+ #
# |                   EssentialsHome                     | #
# +------------------------------------------------------+ #
############################################################
 
# Allows people to set their bed at daytime.
update-bed-at-daytime: ".$_POST['update-bed-at-daytime']."
 
# Set to true to enable per-world permissions for using homes to teleport between worlds.
# This applies to the /home only.
# Give someone permission to teleport to a world with essentials.worlds.<worldname>
world-home-permissions: ".$_POST['world-home-permissions']."
 
# Allow players to have multiple homes.
# Players need essentials.sethome.multiple before they can have more than 1 home.
# You can set the default number of multiple homes using the 'default' rank below.
# To remove the home limit entirely, give people 'essentials.sethome.multiple.unlimited'.
# To grant different home amounts to different people, you need to define a 'home-rank' below.
# Create the 'home-rank' below, and give the matching permission: essentials.sethome.multiple.<home-rank>
# For more information, visit http://wiki.ess3.net/wiki/Multihome
sethome-multiple:
";
foreach($_POST['homes-ranks'] as $a => $b){
  if(!empty($_POST['homes-ranks'][$a])) {
    $output .= "  ".$_POST['homes-ranks'][$a].": ".$_POST['homes-values'][$a]."\n";
  }
}
$output .= "
# In this example someone with 'essentials.sethome.multiple' and 'essentials.sethome.multiple.vip' will have 5 homes.
# Remember, they MUST have both permission nodes in order to be able to set multiple homes.
 
# Set the timeout, in seconds for players to accept a tpa before the request is cancelled.
# Set to 0 for no timeout.
tpa-accept-cancellation: ".$_POST['tpa-accept-cancellation']."
 
############################################################
# +------------------------------------------------------+ #
# |                   EssentialsEco                      | #
# +------------------------------------------------------+ #
############################################################
 
# For more information, visit http://wiki.ess3.net/wiki/Essentials_Economy
 
# Defines the balance with which new players begin. Defaults to 0.
starting-balance: ".$_POST['starting-balance']."
 
# worth-# defines the value of an item when it is sold to the server via /sell.
# These are now defined in worth.yml
 
# Defines the cost to use the given commands PER USE.
# Some commands like /repair have sub-costs, check the wiki for more information.
command-costs:
";
foreach($_POST['command-costs-commands'] as $a => $b){
  if(!empty($_POST['command-costs-commands'][$a])) {
    $output .= "  ".$_POST['command-costs-commands'][$a].": ".$_POST['command-costs-costs'][$a]."\n";
  }
}

$output .= "
# Set this to a currency symbol you want to use.
# Remember, if you want to use special characters in this document, 
# such as accented letters, you MUST save the file as UTF-8, not ANSI.
currency-symbol: '".$_POST['currency-symbol']."'
 
# Set the maximum amount of money a player can have.
# The amount is always limited to 10 trillion because of the limitations of a java double.
max-money: ".$_POST['max-money']."
 
# Set the minimum amount of money a player can have (must be above the negative of max-money).
# Setting this to 0, will disable overdrafts/loans completely.  Users need 'essentials.eco.loan' perm to go below 0.
min-money: ".$_POST['min-money']."
 
# Enable this to log all interactions with trade/buy/sell signs and sell command.
economy-log-enabled: ".$_POST['economy-log-enabled']."

# Use this option to force superperms-based permissions handler regardless of detected installed perms plugin.
# This is useful if you want superperms-based permissions (with wildcards) for custom permissions plugins.
# Default is false. 
use-bukkit-permissions: ".$_POST['use-bukkit-permission']."

# Minimum acceptable amount to be used in /pay.
minimum-pay-amount: ".$_POST['minimum-pay-amount']."
 
############################################################
# +------------------------------------------------------+ #
# |                   EssentialsHelp                     | #
# +------------------------------------------------------+ #
############################################################
 
# Show other plugins commands in help.
non-ess-in-help: ".$_POST['non-ess-in-help']."
 
# Hide plugins which do not give a permission.
# You can override a true value here for a single plugin by adding a permission to a user/group.
# The individual permission is: essentials.help.<plugin>, anyone with essentials.* or '*' will see all help regardless.
# You can use negative permissions to remove access to just a single plugins help if the following is enabled.
hide-permissionless-help: ".$_POST['hide-permissionless-help']."
 
############################################################
# +------------------------------------------------------+ #
# |                   EssentialsChat                     | #
# +------------------------------------------------------+ #
############################################################
 
# This section requires the EssentialsChat.jar to work.
 
chat:

  # If EssentialsChat is installed, this will define how far a player's voice travels, in blocks.  Set to 0 to make all chat global.
  # Note that users with the \"essentials.chat.spy\" permission will hear everything, regardless of this setting.
  # Users with essentials.chat.shout can override this by prefixing text with an exclamation mark (!)
  # Users with essentials.chat.question can override this by prefixing text with a question mark (?)
  # You can add command costs for shout/question by adding chat-shout and chat-question to the command costs section.\"
  radius: ".$_POST['chat-radius']."
 
  # Chat formatting can be done in two ways, you can either define a standard format for all chat.
  # Or you can give a group specific chat format, to give some extra variation.
  # For more information of chat formatting, check out the wiki: http://wiki.ess3.net/wiki/Chat_Formatting
 
  format: '".$_POST['chat-format']."'
  #format: '&7[{GROUP}]&r {DISPLAYNAME}&7:&r {MESSAGE}'
 
  group-formats:
";
if($_POST['chat-group-enabled'] == "true") {
  foreach ($_POST['chat-group-groups'] as $a => $b) {
    if(!empty($_POST['chat-group-groups'][$a])) {
      $output .= "    ".$_POST['chat-group-groups'][$a].": '".$_POST['chat-group-formats'][$a]."'";
    }
  }
}
if($_POST['chat-group-enabled'] == "false") {
 $output .= "  #  Default: '{WORLDNAME} {DISPLAYNAME}&7:&r {MESSAGE}'\n  #  Admins: '{WORLDNAME} &c[{GROUP}]&r {DISPLAYNAME}&7:&c {MESSAGE}'";
}

 $output .= "
  # If you are using group formats make sure to remove the '#' to allow the setting to be read.
 
############################################################
# +------------------------------------------------------+ #
# |                 EssentialsProtect                    | #
# +------------------------------------------------------+ #
############################################################
 
# This section requires the EssentialsProtect.jar to work.
 
protect:

  # General physics/behavior modifications.
  prevent:
    lava-flow: ".$_POST['lava-flow']."
    water-flow: ".$_POST['water-flow']."
    water-bucket-flow: ".$_POST['water-bucket-flow']."
    fire-spread: ".$_POST['fire-spread']."
    lava-fire-spread: ".$_POST['lava-fire-spread']."
    flint-fire: ".$_POST['flint-fire']."
    lightning-fire-spread: ".$_POST['lightning-fire-spread']."
    portal-creation: ".$_POST['portal-creation']."
    tnt-explosion: ".$_POST['tnt-explosion']."
    tnt-playerdamage: ".$_POST['tnt-playerdamage']."
    tnt-minecart-explosion: ".$_POST['tnt-minecart-explosion']."
    tnt-minecart-playerdamage: ".$_POST['tnt-minecart-playerdamage']."
    fireball-explosion: ".$_POST['fireball-explosion']."
    fireball-fire: ".$_POST['fireball-fire']."
    fireball-playerdamage: ".$_POST['fireball-playerdamage']."
    witherskull-explosion: ".$_POST['witherskull-explosion']."
    witherskull-playerdamage: ".$_POST['witherskull-playerdamage']."
    wither-spawnexplosion: ".$_POST['wither-spawnexplosion']."
    wither-blockreplace: ".$_POST['wither-blockreplace']."
    creeper-explosion: ".$_POST['creeper-explosion']."
    creeper-playerdamage: ".$_POST['creeper-playerdamage']."
    creeper-blockdamage: ".$_POST['creeper-blockdamage']."
    enderdragon-blockdamage: ".$_POST['enderdragon-blockdamage']."
    enderman-pickup: ".$_POST['enderman-pickup']."
    villager-death: ".$_POST['villager-death']."
    # Monsters won't follow players.
    # permission essentials.protect.entitytarget.bypass disables this.
    entitytarget: ".$_POST['entitytarget']."
    # Prevent the spawning of creatures.
    spawn:
      creeper: ".$_POST['spawn-creeper']."
      skeleton: ".$_POST['spawn-skeleton']."
      spider: ".$_POST['spawn-spider']."
      giant: ".$_POST['spawn-giant']."
      zombie: ".$_POST['spawn-zombie']."
      slime: ".$_POST['spawn-slime']."
      ghast: ".$_POST['spawn-ghast']."
      pig_zombie: ".$_POST['spawn-pig_zombie']."
      enderman: ".$_POST['spawn-enderman']."
      cave_spider: ".$_POST['spawn-cave_spider']."
      silverfish: ".$_POST['spawn-silverfish']."
      blaze: ".$_POST['spawn-blaze']."
      magma_cube: ".$_POST['spawn-magma_cube']."
      ender_dragon: ".$_POST['spawn-ender_dragon']."
      pig: ".$_POST['spawn-pig']."
      sheep: ".$_POST['spawn-sheep']."
      cow: ".$_POST['spawn-cow']."
      chicken: ".$_POST['spawn-chicken']."
      squid: ".$_POST['spawn-squid']."
      wolf: ".$_POST['spawn-wolf']."
      mushroom_cow: ".$_POST['spawn-mushroom_cow']."
      snowman: ".$_POST['spawn-snowman']."
      ocelot: ".$_POST['spawn-ocelot']."
      iron_golem: ".$_POST['spawn-iron_golem']."
      villager: ".$_POST['spawn-villager']."
      wither: ".$_POST['spawn-wither']."
      bat: ".$_POST['spawn-bat']."
      witch: ".$_POST['spawn-witch']."
      horse: ".$_POST['spawn-horse']."
 
  # Maximum height the creeper should explode. -1 allows them to explode everywhere.
  # Set prevent.creeper-explosion to true, if you want to disable creeper explosions.
  creeper:
    max-height: ".$_POST['creeper-max-height']."
 
  # Disable various default physics and behaviors.
  disable:
    # Should fall damage be disabled?
    fall: ".$_POST['disable-fall-damage']."
 
    # Users with the essentials.protect.pvp permission will still be able to attack each other if this is set to true.
    # They will be unable to attack users without that same permission node.
    pvp: ".$_POST['disable-pvp-damage']."
 
    # Should drowning damage be disabled?
    # (Split into two behaviors; generally, you want both set to the same value.)
    drown: ".$_POST['disable-drowning']."
    suffocate: ".$_POST['disable-suffocation']."
 
    # Should damage via lava be disabled?  Items that fall into lava will still burn to a crisp. ;)
    lavadmg: ".$_POST['disable-lava-damage']."
 
    # Should arrow damage be disabled?
    projectiles: ".$_POST['disable-projectile-damage']."
 
    # This will disable damage from touching cacti.
    contactdmg: ".$_POST['disable-contact-damage']."
 
    # Burn, baby, burn!  Should fire damage be disabled?
    firedmg: ".$_POST['disable-fire-damage']."
 
    # Should the damage after hit by a lightning be disabled?
    lightning: ".$_POST['disable-lightning-damage']."
 
    # Should Wither damage be disabled?
    wither: ".$_POST['disable-wither-damage']."
 
    # Disable weather options?
    weather:
      storm: ".$_POST['disable-storms']."
      thunder: ".$_POST['disable-thunder']."
      lightning: ".$_POST['disable-lightning']."
 
############################################################
# +------------------------------------------------------+ #
# |                EssentialsAntiBuild                   | #
# +------------------------------------------------------+ #
############################################################
 
  # This section requires the EssentialsAntiBuild.jar to work.
 
  # Disable various default physics and behaviors
  # For more information, visit http://wiki.ess3.net/wiki/AntiBuild
 
    # Should people with build: false in permissions be allowed to build?
    # Set true to disable building for those people.
    # Setting to false means EssentialsAntiBuild will never prevent you from building.
    build: ".$_POST['build-privilege']."
 
    # Should people with build: false in permissions be allowed to use items?
    # Set true to disable using for those people.
    # Setting to false means EssentialsAntiBuild will never prevent you from using items.
    use: ".$_POST['use-privilege']."
 
    # Should we tell people they are not allowed to build?
    warn-on-build-disallow: ".$_POST['warn-on-build-disallow']."
 
  # For which block types would you like to be alerted?
  # You can find a list of IDs in plugins/Essentials/items.csv after loading Essentials for the first time.
  # 10 = lava :: 11 = still lava :: 46 = TNT :: 327 = lava bucket
  alert:
    on-placement: ".$_POST['alert-on-placement']."
    on-use: ".$_POST['alert-on-use']."
    on-break: ".$_POST['alert-on-break']."

  blacklist:

    # Which blocks should people be prevented from placing?
    placement: ".$_POST['blacklist-on-placement']."
 
    # Which items should people be prevented from using?
    usage: ".$_POST['blacklist-on-use']."
 
    # Which blocks should people be prevented from breaking?
    break: ".$_POST['blacklist-on-break']."

    # Which blocks should not be pushed by pistons?
    piston: ".$_POST['blacklist-on-piston']."

    # Which blocks should not be dispensed by dispensers
    dispenser: ".$_POST['blacklist-on-dispenser']."

############################################################
# +------------------------------------------------------+ #
# |            Essentials Spawn / New Players            | #
# +------------------------------------------------------+ #
############################################################
 
# This section requires essentialsspawn.jar to work.
 
newbies:
  # Should we announce to the server when someone logs in for the first time?
  # If so, use this format, replacing {DISPLAYNAME} with the player name.
  # If not, set to ''
  #announce-format: ''
  announce-format: '".$_POST['welcome-message']."'
 
  # When we spawn for the first time, which spawnpoint do we use?
  # Set to \"none\" if you want to use the spawn point of the world.
  spawnpoint: ".$_POST['newplayer-spawnpoint']."
 
  # Do we want to give users anything on first join? Set to '' to disable
  # This kit will be given regardless of cost and permissions, and will not trigger the kit delay.
  #kit: ''
  kit: ";
  if(empty($_POST['newplayer-spawnpoint'])) {
    $output .= "''";
  } else {
    $output .= $_POST['newplayer-spawnpoint'];
  }
$output .= "

# Set this to lowest, if you want Multiverse to handle the respawning.
# Set this to high, if you want EssentialsSpawn to handle the respawning.
# Set this to highest, if you want to force EssentialsSpawn to handle the respawning.
respawn-listener-priority: ".$_POST['respawn-listener-priority']."
 
# When users die, should they respawn at their first home or bed, instead of the spawnpoint?
respawn-at-home: ".$_POST['respawn-at-home']."

# Teleport all joining players to the spawnpoint
spawn-on-join: ".$_POST['spawn-on-join']."
 
# End of file <-- No seriously, you're done with configuration.
";

function rnd_id($length){
//set the random id length 
$random_id_length = $length; 

//generate a random id encrypt it and store it in $rnd_id 
$rnd_id = crypt(uniqid(rand(),1)); 

//to remove any slashes that might have come 
$rnd_id = strip_tags(stripslashes($rnd_id)); 

//Removing any . or / and reversing the string 
$rnd_id = str_replace(".","",$rnd_id); 
$rnd_id = strrev(str_replace("/","",$rnd_id)); 

//finally I take the first 10 characters from the $rnd_id 
$rnd_id = substr($rnd_id,0,$random_id_length);

return $rnd_id;
 }

$hash = rnd_id(8);
?>